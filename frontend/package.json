{
  "name": "auroria-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "npm run copy-files",
    "copy-files": "if not exist dist mkdir dist && xcopy *.html dist\\ && xcopy *.css dist\\ && xcopy *.js dist\\ && xcopy node_modules dist\\node_modules\\ /E /I /Y",
    "serve": "npx serve dist -p 5173",
    "preview": "npm run build && npm run serve"
  },
  "dependencies": {
    "express": "^4.19.2",
    "lamejs": "^1.2.0",
    "browser-id3-writer": "^4.1.0",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}
